version: '3.8'

services:
  app-status:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app-status-service
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
